# {{项目名称}} 需求文档 (PRD)

> **当前版本**: v{{版本号}} | **沟通轮次**: 第{{轮次}}次 | **更新日期**: {{日期}}
> 
> **版本变更**: {{上一个版本}} → {{当前版本}} {{变更说明}}
|
---

## 1. 项目概述

### 1.1 产品定位
{{产品定位描述}}

### 1.2 核心价值
- **{{核心价值1}}**：{{描述}}
- **{{核心价值2}}**：{{描述}}
- **{{核心价值3}}**：{{描述}}

---

## 2. 技术架构

### 2.1 技术栈
- **后端**：{{后端技术}}
- **前端**：{{前端技术}}
- **AI**（可选）：{{AI服务}}
- **数据存储**：
  - JSON 文件
  - SQLite 数据库（可选）

### 2.2 目录结构
```
{{项目根目录}}/
├── public/                 # 前端静态资源
│   ├── index.html         # 主页面
│   ├── css/style.css      # 样式文件
│   └── js/                # JavaScript文件
├── data/                   # 数据存储（可选）
├── server.js              # 后端服务
├── config.json            # 配置文件
└── package.json           # 依赖配置
```

---

## 3. 页面功能说明

### 3.1 {{页面名称}} ({{文件名}}.html)

#### UI 布局
```
┌─────────────────────────────────┐
│         页面布局示意图           │
└─────────────────────────────────┘
```

#### 组件说明
| 组件 | 描述 |
|------|------|
| {{组件1}} | {{描述}} |
| {{组件2}} | {{描述}} |

#### 功能逻辑
1. **{{功能1}}**：{{描述}}
2. **{{功能2}}**：{{描述}}

#### 交互状态
| 状态 | 表现 |
|------|------|
| 默认 | {{表现}} |
| 加载中 | {{表现}} |
| 错误 | {{表现}} |

---

## 4. 核心功能流程

### 4.1 {{流程名称}}
```
{{流程步骤1}} → {{流程步骤2}} → {{流程步骤3}}
```

---

## 5. API 接口文档

### 5.1 {{模块}}相关
| 接口 | 方法 | 参数 | 返回 |
|------|------|------|------|
| `/api/{{接口名}}` | POST | `{param}` | `{data}` |

---

## 6. 数据结构

### 6.1 {{数据文件}}.json
```json
{
  "{{key}}": [
    {
      "id": "{{唯一标识}}",
      "name": "{{名称}}"
    }
  ]
}
```

---

## 7. 开发规范

### 7.1 代码风格
- 使用 {{代码风格规范}}（如 ESLint、Prettier）
- 命名规范：{{命名规范描述}}

### 7.2 Git 规范
- 分支命名：`feature/{{功能名}}`、`bugfix/{{问题}}`
- 提交信息：`[类型] {{简短描述}}`

### 7.3 文档规范
- 先改文档，用户确认后再写代码
- 代码完成后更新变更日志

---

## 8. 后续迭代方向

### 8.1 功能增强
- [ ] {{功能1}}
- [ ] {{功能2}}

### 8.2 用户体验
- [ ] {{体验优化1}}
- [ ] {{体验优化2}}

### 8.3 技术债务
- [ ] {{技术改进1}}
- [ ] {{技术改进2}}
