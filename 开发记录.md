# 开发记录

## v1.6 Markdown渲染支持（10轮次）

### 功能需求
- AI气泡支持Markdown内容渲染
- 引入 marked.js 库轻量化实现

### 任务清单
- [x] 功能1：引入 marked.js CDN
- [x] 功能2：修改 `addMessage` 函数，AI消息使用 `marked.parse()` 渲染
- [x] 功能3：添加 Markdown 样式（code、pre、a、ul、blockquote等）

### 修改文件清单
| 文件 | 修改内容 |
|------|---------|
| `public/index.html` | 引入 marked.js CDN |
| `public/js/chat.js` | AI消息渲染使用 `marked.parse()` |
| `public/css/style.css` | 添加 Markdown 元素样式 |

### 技术方案
```javascript
// chat.js - addMessage 函数
if (role === 'ai') {
    renderedContent = marked.parse(content);
} else {
    renderedContent = escapeHtml(content);
}
```

### 依赖
- marked.js CDN：`https://cdn.jsdelivr.net/npm/marked/marked.min.js`

---

## v1.5 Welcome区域手机端居中（09轮次）

### Bug修复
- [x] Bug1：Welcome 区域在手机端不水平居中

### 排查过程（4轮）

| 轮次 | 尝试方案 | 问题 | 结果 |
|------|----------|------|------|
| 1 | `.welcome-screen` 添加 `width: 100%` | 误以为是 flex 容器需要宽度 | ❌ 未生效 |
| 2 | 添加 `max-width: 600px` + `margin: 0 auto` | 方向正确但未理解本质 | ❌ 未生效 |
| 3 | 修改父容器 `.chat-container` 添加 `display: flex; align-items: center` | **错误理解！**导致所有内容包括气泡都居中了 | ❌ 大错特错 |
| 4 | 恢复到 `.welcome-screen`，改为 `text-align: center` | 理解问题本质：只需要文字居中 | ✅ 成功 |

### 问题根因
用户说"区域居中"实际需要的是 **内容居中**，而非容器居中。

### 最终解决方案
```css
.welcome-screen {
    text-align: center;
    padding-top: 40px;
    padding-bottom: 20px;
}
```

### 经验教训
1. **理解需求要准确**：用户说"区域居中"不一定是容器居中
2. **避免过度修改**：第3轮修改了父容器，导致副作用（气泡也居中了）
3. **从问题本质出发**：welcome 区域本质是文字块，`text-align: center` 才是正确方案

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `public/css/style.css` | `.welcome-screen` 简化为 `text-align: center` |

---

## v1.5 视频卡片缩略图优化（08轮次）

### 任务清单

#### 功能实现
- [x] 功能1：Python脚本生成视频缩略图（OpenCV方案）
- [x] 功能2：服务端启动时检测并生成缺失缩略图
- [x] 功能3：API返回视频列表时携带缩略图路径

#### 样式优化
- [x] 优化1：视频卡片缩略图显示真实图片（16:9比例）
- [x] 优化2：播放按钮居中显示（圆形半透明背景）
- [x] 优化3：标题最多2行，超出省略

#### Bug修复
- [x] Bug1：缩略图不显示（API未返回thumbnail字段）
- [x] Bug2：标题显示超过2行（添加word-break和min-height）

#### 文档更新
- [x] 更新沟通记录.md
- [x] 更新变更日志.md
- [x] 更新开发记录.md（本文件）

### 修改文件清单

| 文件 | 修改内容 |
|------|---------|
| `generate_thumbnails.py` | 新增Python脚本，用OpenCV提取视频首帧 |
| `public/css/style.css` | 视频卡片缩略图样式优化（16:9、居中播放按钮） |
| `public/js/chat.js` | 视频卡片渲染使用真实缩略图 |
| `server.js` | searchVideos函数返回thumbnail字段 |

### 技术方案

```
┌─────────────────────────────────────────────┐
│  服务端启动时                                │
│  python generate_thumbnails.py               │
│         ↓                                   │
│  读取 videos.json                           │
│         ↓                                   │
│  检测无缩略图的视频                          │
│         ↓                                   │
│  用 OpenCV 提取视频首帧                      │
│         ↓                                   │
│  保存到 public/thumbnails/{id}.jpg          │
│         ↓                                   │
│  更新 videos.json 的 thumbnail 字段          │
└─────────────────────────────────────────────┘
```

### 后续维护

- 新增视频后运行：`python generate_thumbnails.py`
- 依赖安装：`pip install opencv-python`

---

## v1.4 UI优化迭代（06轮次）

### 任务清单

#### 样式优化
- [x] 优化1：用户消息气泡背景色改为 #2577FF（主题色）
- [x] 优化2：录音动画主题色改为 #2577FF
- [x] 优化3：AI消息气泡圆角改为 ~4% 宽度
- [x] 优化4：用户消息气泡圆角改为 ~4% 宽度

### 修改文件清单
| 文件 | 修改内容 |
|---|---|
| `public/css/style.css` | 主色调、用户气泡色、圆角、录音动画、发送按钮等全部改为 #2577FF |
| `public/js/chat.js` | 无需修改（样式通过CSS变量统一控制） |

---

## v1.3 UI优化迭代（05轮次）

### 任务清单

#### 新增功能
- [x] 功能1：消息工具栏（复制、语音、收藏、刷新）
- [x] 功能2：空白区域点击切换UI模式
- [x] 功能3：收藏消息到我的收藏页面

#### 样式优化
- [x] 优化1：工具栏样式（#F5F6F7背景、#4A90D9图标色）

#### Bug修复
- [x] Bug1：工具栏布局错误（左右→垂直，工具栏放入气泡内部）
- [x] Bug2：空白区域点击无反应（类名不匹配 .welcome-section → .welcome-screen）
- [x] Bug3：空白区域点击直接开始录音（应只切换UI，不调用startRecording）
- [x] 修复4：删除顶部重复的历史记录按钮

#### 文档更新
- [x] 更新沟通记录.md
- [ ] 更新PRD.md
- [x] 更新变更日志.md
- [x] 更新开发记录.md（本文件）

### 修改文件清单
| 文件 | 修改内容 |
|---|---|
| `public/css/style.css` | 新增消息工具栏样式 |
| `public/js/chat.js` | 添加工具栏功能、空白区域点击逻辑 |
| `public/index.html` | 删除重复按钮 |

---

## v1.3 UI优化迭代

### 任务清单

#### Bug修复
- [x] 修复1：长按说话功能失效（绑定到inputHint）
- [x] 修复2：视频卡片推荐提示词消失

#### 样式优化
- [x] 优化1：快捷功能按钮样式（白色背景+边框）
- [x] 优化2：底部输入区域样式（白色背景+边框）

#### 新增功能
- [x] 功能1：打字/说话模式切换逻辑
- [x] 功能2：语音识别波浪动画效果
- [x] 功能3：Web Speech API语音转文字

#### 文档更新
- [x] 更新PRD.md
- [x] 更新变更日志.md
- [x] 更新沟通记录.md
- [x] 更新README.md版本号

### 修改文件清单
| 文件 | 修改内容 |
|---|---|
| `public/index.html` | 底部输入区域HTML结构 |
| `public/css/style.css` | 白色胶囊风格样式、波浪动画CSS |
| `public/js/chat.js` | 长按事件绑定、波浪动画、视频推荐提示词 |

---

## 2026-05-04 迭代开发（v1.2）

### 一、本期需求清单

| 序号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| 1 | UI风格整体调整 | P0 | ✅ 完成 |
| 2 | 底部聊天框 redesign | P0 | ✅ 完成 |
| 3 | 提示词推荐功能 | P0 | ✅ 完成 |
| 4 | 聊天间隔优化 | P1 | ✅ 完成 |
| 5 | 视频卡片优化 | P0 | ✅ 完成 |

### 二、代码修改清单

| 文件 | 修改内容 |
|---|---|
| `public/css/style.css` | 豆包风格UI样式、输入框 redesign、视频卡片样式 |
| `public/index.html` | 调整HTML结构、添加提示词推荐区域 |
| `public/js/chat.js` | 实现提示词推荐逻辑、输入框事件处理 |
| `server.js` | 新增 `/api/prompt-suggestions` 端点 |
| `PRD.md` | 记录本期需求文档 |

### 三、待办事项

- [x] 测试验证新UI效果
- [x] 更新 README.md 版本号

---

## 2026-04-26 Bug修复

### 一、问题修复清单

| 问题 | 原因 | 解决方案 | 状态 |
|---|---|---|---|
| 历史记录页面视频点击无反应 | 事件冒泡问题 | 改用独立div+点击事件 | ✅ |
| 历史记录页面无法向下滑动 | 滚动区域问题 | 设置overflow-y: auto | ✅ |
| 历史记录中AI头像显示异常 | 使用emoji而非图片 | 改用doubao.webp图片 | ✅ |
| 视频播放路径不统一 | 架构设计不清晰 | 统一走本地路径 | ✅ |
| 大模型字符量过大 | 传输完整路径 | 只传 `{id, name}` | ✅ |
| 关键词提取错误 | 未移除"视频"关键词 | 改进正则表达式 | ✅ |
| playVideo 关键词获取错误 | 从错误位置获取 | 直接用视频ID请求 | ✅ |
| 视频ID不稳定 | 使用数组索引 | 使用固定ID字符串 | ✅ |

---

## 2026-04-25 视频播放重构问题

| 问题 | 原因 | 解决方案 |
|---|---|---|
| 视频播放路径不统一 | 架构设计不清晰 | 统一走本地路径 |
| 大模型字符量过大 | 传输完整路径 | 只传 `{id, name}` |
| 关键词提取错误 | 未移除"视频"关键词 | 改进正则表达式 |
| playVideo 关键词获取错误 | 从错误位置获取 | 直接用视频ID请求 |
| 视频ID不稳定 | 使用数组索引 | 使用固定ID字符串 |
| 文件名包含特殊字符 | 全角冒号等影响匹配 | 规范命名 |


---

## v1.5 Bug修复（第二轮）

### Bug修复清单

#### Bug1：缩略图文件名乱码
| 项目 | 内容 |
|------|------|
| 问题 | 视频 ID 包含中文，保存为文件名时乱码 |
| 原因 | 文件系统编码问题 |
| 解决方案 | 使用 MD5 哈希生成 16 位安全文件名 |

#### Bug2：手机端标题显示3行
| 项目 | 内容 |
|------|------|
| 问题 | 宽屏2行，手机3行 |
| 原因 | `-webkit-line-clamp: 2` 在小屏幕下不生效 |
| 解决方案 | CSS 添加固定高度 `height: 36px` |

### 修改文件
| 文件 | 修改内容 |
|------|---------|
| `generate_thumbnails.py` | 添加 `safe_filename()` 函数 |
| `style.css` | 标题 `.video-card-title-inline` 添加 `height: 36px` |

### 后续操作

- [ ] 重新运行 `python generate_thumbnails.py` 生成正确文件名的缩略图
- [ ] 手动删除乱码的缩略图文件


---

## 完成确认

### 代码修改
- [x] `generate_thumbnails.py` - 添加 `safe_filename()` 函数
- [x] `style.css` - 标题 `.video-card-title-inline` 添加 `height: 36px`

### 后续操作
- [x] 清理旧缩略图文件
- [x] 清空 `videos.json` 的 thumbnail 字段
- [x] 重新运行 `python generate_thumbnails.py` 生成新缩略图

### 验证结果
缩略图文件名已使用 MD5 哈希，安全无乱码。
